- 2026-08-13 · [演进] 将 Z.AI 图片能力作为受控本地 AiTool 接入 dsChat。范围：package:ds-chat
  原因：在不支持直接图片输入的 Provider 上提供截图 OCR、错误诊断和通用图片分析，同时保持附件来源、工具执行和 Provider 协议边界可控。
  当前依据：`architecture/packages/ds-chat.md`、`requirements/CONTEXT.md`、ADR-0001、ADR-0002。
  证据：`src/services/tools/zaiClient.ts`、`src/services/tools/zaiImageTools.ts`、`src/services/ai/toolOrchestrator.ts`、`src/composables/chatAppReplyLifecycle.ts`、`src/composables/chatAppToolSettings.ts`。
- 2026-08-14 · [缺陷] 启用 Z.AI 图片工具时优先剥离 Provider 图片附件，避免错误能力配置或旧设置导致图片直发文本模型。范围：package:ds-chat
  原因：Provider 返回的图片输入错误证明直发路径仍可能被能力开关放行；Z.AI 工具已是用户明确选择的图片处理边界。
  当前依据：`architecture/packages/ds-chat.md`、`requirements/CONTEXT.md`、ADR-0002。
  证据：`src/composables/chatAppReplyLifecycle.ts`、`src/composables/chatAppReplyLifecycle.spec.ts`。
- 2026-08-14 · [缺陷] 修复图片会话标题被模型误答为“请提供具体内容”。范围：package:ds-chat
  原因：标题请求只给文本模型发送了图片相关问题，未声明其不需要查看图片；模型将标题任务误识别为缺少待总结正文。
  当前依据：`architecture/packages/ds-chat.md`、`requirements/CONTEXT.md`。
  证据：`src/services/conversationTitle.ts`、`src/services/conversationTitle.spec.ts`。
- 2026-08-14 · [缺陷] 将 Z.AI 图片工具单次执行上限从 20 秒调整为 60 秒，普通工具和总流程上限不变。范围：package:ds-chat
  原因：GLM 视觉请求可能超过通用工具的 20 秒窗口；此前截图中的 `zai_analyze_image` 超时发生在工具执行边界，而不是图片直发 Provider。
  当前依据：`architecture/packages/ds-chat.md`、`requirements/CONTEXT.md`、ADR-0002。
  证据：`src/services/ai/toolExecution.ts`、`src/services/ai/toolOrchestrator.ts`、`src/services/ai/toolOrchestrator.spec.ts`。
- 2026-08-14 · [演进] 将图片识别后端从 Z.AI 替换为阿里云百炼 Qwen `qwen3-vl-flash`。范围：package:ds-chat
  原因：Z.AI 视觉请求延迟过高，用户明确选择阿里云模型；Qwen 通过 DashScope OpenAI 兼容接口可以复用现有受控工具链。
  当前依据：`architecture/packages/ds-chat.md`、`requirements/CONTEXT.md`、ADR-0002。
  重要边界：旧 `zaiImage` 设置只迁移启用意图，不迁移旧 API Key；新工具默认使用 DashScope `/compatible-mode/v1` 基础地址并自动追加 `/chat/completions`，模型为 `qwen3-vl-flash`，并显式关闭 Qwen thinking 模式以降低延迟。
  证据：`src/services/tools/qwenClient.ts`、`src/services/tools/qwenImageTools.ts`、`src/composables/chatAppToolSettings.ts`、`src/components/SettingsToolsSection.vue`。
- 2026-08-14 · [重构] 收拢回复请求计划和设置编辑接线。范围：package:ds-chat
  原因：回复生命周期中的工具、图片和 Provider 路由规则分散；设置字段需要穿过多个组件和组合根转发。
  当前依据：`architecture/packages/ds-chat.md`、`requirements/CONTEXT.md`。
  结果：新增 `chatAppReplyPlan.ts` 统一生成回合计划；`AiTool` 元数据统一声明图片附件要求和执行超时；设置面板统一发 `SettingsEdit`，应用只暴露 `applySettingsEdit`。
  证据：`src/composables/chatAppReplyPlan.ts`、`src/services/ai/toolTypes.ts`、`src/components/SettingsPanel.vue`、`src/types/settingsPanel.ts`、`npm run check`。
- 2026-08-31 · [缺陷] 要求时效性 Tavily 查询先调用当前时间工具，再用时间结果构造网页搜索条件。范围：package:ds-chat
  原因：模型可能把训练数据中的旧年份带入联网查询；时间结果应只服务于当前工具轮次，不应成为普通回答的全局上下文。
  当前依据：`architecture/packages/ds-chat.md`、`requirements/CONTEXT.md`。
  证据：`src/services/tools/currentTimeTool.ts`、`src/services/tools/tavilySearchTool.ts`、`src/services/ai/systemPrompt.ts`。
