- 2026-09-14 · [重构] 将 Provider/模型事实收拢到画像表，并把本回合工具解析与 uTools 宿主生命周期提成独立边界。范围：package:ds-chat
  原因：能力默认值、协议互斥与模型级事实分散在多份常量、设置归一化和界面组件；工具解析在回合计划与编排器中重复执行；uTools 进入/退出与恢复逻辑混在应用组合根中。
  当前依据：`architecture/packages/ds-chat.md`；新增模型 ID 解析规则同时更新 `requirements/CONTEXT.md`。
  重要边界：模型 ID 解析改为忽略大小写并先归一并保留非预设模型（例如 `gpt-5.4`）的能力档案；协议互斥规则改由设置编辑层统一执行，界面只发单次编辑；编排器不再解析工具设置。
  证据：`src/constants/providerProfiles.ts`、`src/composables/chatAppTurnPlan.ts`、`src/services/tools/toolRegistry.ts`、`src/composables/utoolsHostLifecycle.ts`、`npm run check`。
- 2026-09-14 · [演进] 单次工具执行失败不再终止回合，而是作为工具结果交回模型继续本轮。范围：package:ds-chat
  原因：外部工具（如 Tavily 返回 HTTP 5xx）的瞬时失败会丢弃整轮已取得的工具结果并让用户完全拿不到回答；失败轨迹本来已经可见，终止回合并非“显式暴露”的唯一形式。
  当前依据：`requirements/CONTEXT.md`「回复与工具」、`architecture/packages/ds-chat.md` 关键不变量与 `toolOrchestrator.ts` 条目。
  重要边界：只有工具自身的执行失败（外部服务、传输错误或工具自身参数校验失败）被降级为工具结果；用户停止、单次/总流程超时、模型给出的工具参数无法解析、未知工具、重复调用和缺失最终回答仍是硬失败。
  证据：`src/services/ai/toolExecution.ts`、`src/services/ai/toolOrchestrator.spec.ts`、`npm test`；uTools 实机用错误参数调用 `tavily_search` / `get_current_time`，回合继续并回传两条失败信息。
- 2026-09-14 · [缺陷] 过程时间线不再把推理压缩成摘要，展开过程面板可以看到完整思考。范围：package:ds-chat
  原因：619b454 按当时 `docs/tool-calling-architecture.md` 的界面规则“不要展示完整模型内部思维链，只展示工具阶段状态和工具名”把推理压平成 180 字以内、去掉换行的单行文本；用户展开面板只能看到两行并以省略号截断，而折叠态本来就不渲染面板内容，摘要没有展示价值。
  当前依据：`architecture/packages/ds-chat.md`「UI 与应用状态」。
  重要边界：推理条目保存完整推理文本并按轮次保留，`reasoningContent` 仍是 Provider 回显来源，两者并存；展开态按内容自然展开，不设高度上限或内部滚动，长过程由消息列表滚动承载。工具行不受此变更影响，仍是阶段状态、工具名与结果条数摘要。
  证据：`src/services/ai/toolTimelineNarration.ts`、`src/utils/messageBubble.ts`、`src/components/MessageBubble.dom.spec.ts`、`src/services/ai/toolOrchestrator.spec.ts`、`npm run check`。
