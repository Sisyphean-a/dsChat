# Domain Glossary

## Conversations

- **会话管理** — 创建、选择、保存和维护对话会话及其元信息的能力，不负责生成流式回复内容。
- **流式回复** — 模型回复以可见增量逐步到达的对话回复。它在完成、停止或中断前都属于同一条回复。
- **已停止的流式回复** — 用户主动停止、尚未完成的流式回复；它不同于 Provider 请求失败。
- **统一会话消息** — 与具体 Provider 无关的对话消息表示，可携带用户、助手与工具结果。
- **工具轮次** — 流式回复中，模型提出工具调用、得到工具结果后再次请求模型的一次循环。
- **工具调用** — 模型在流式回复中请求执行具名工具的结构化指令，包含工具标识与参数。
- **工具调用批次** — 同一工具轮次中模型给出的全部完整工具调用，作为一条事件交给工具编排。
- **工具定义** — 供模型识别可调用工具的结构化描述，不包含工具的实际执行结果。
- **Provider 请求失败** — Provider 在处理一次流式回复时发生的传输、响应或协议失败；它不是用户主动停止。
- **原生联网搜索** — Provider 自己提供的联网搜索能力，不触发本地工具调用或工具轮次。
- **空流式结果** — 不包含文本增量或工具调用的已结束流式回复；它属于 Provider 请求失败。
- **最终回答** — 工具轮次完成后模型给出的最后一段非空文本；缺失时该回复不是成功结果。
- **流式状态** — 流式回复期间可展示的进度信息，不改变回复文本或工具调用。
- **Provider 流** — 将一次统一会话消息请求转化为有序流式事件的能力；它不执行工具轮次。
- **工具轨迹** — 工具轮次中一次工具调用从计划、执行到完成、失败或用户停止的可观察记录。
